{
  "name": "hermes-kanban-dashboard-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Kanban dashboard plugin for Hermes Agent — multi-agent collaboration board",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "vite build --watch"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^6.0.0",
    "vite": "^8.0.0",
    "vitest": "^3.0.0"
  }
}
